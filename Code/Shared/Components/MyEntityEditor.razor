@foreach (var p in Entity?.GetType().GetProperties() ?? []) {
    <MyPropertyEditor TValue="object" Item="Entity" PropertyName="@p.Name" />
}

@code {
    [Parameter] public object Entity { get; set; }
}
