@page "/countrycurrencies/delete"
@attribute [ExcludeFromInteractiveRouting]
@using Abc.Data
@using Abc.Infra
@inject ICountryCurrenciesRepo repo
@inject NavigationManager NavigationManager

<MyDeletePage PageTitleText="Delete Country Currency" EntityTitle="Country Currency" Entity="item" BasePath="/countrycurrencies" OnValidSubmit="DeleteItem" />

@code {
    private CountryCurrency item;

    [SupplyParameterFromQuery] private Guid Id { get; set; }

    protected override async Task OnInitializedAsync() {
        item = await repo.GetAsync(Id);
        if (item is null) NavigationManager.NotFound();
    }

    private async Task DeleteItem() {
        await repo.DeleteAsync(item!.Id);
        NavigationManager.NavigateTo("/countrycurrencies");
    }
}
